class StartGame {
  
  void display() {
    fill(0,255,0);
    textSize(135);
    textAlign(CENTER);
    text("Double Hattrick - 1", width/2, height/2);
    fill(255,0,0);
    textSize(50);
    textAlign(CENTER);
    text("Press 'a' key to start", width * 0.5, height * 0.7);
  }
}
